<UserControl x:Class="MyShows.UI.Views.Torrent.List"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Grid.Column="1" Margin="5"  />
        <TextBlock Text="{Binding File}" Grid.Column="1" Grid.Row="1" Margin="5"  />
        <TextBlock Text="{Binding IsDownloaded}" Grid.RowSpan="2" FontSize="20px" Grid.Column="0" Margin="5" />
    </Grid>
</UserControl>
